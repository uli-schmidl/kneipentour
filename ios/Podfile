platform :ios, '15.0'

require File.expand_path('../.ios/Flutter/podhelper.rb', __dir__)

target 'Runner' do
  flutter_ios_podfile_setup
  flutter_install_all_ios_pods(File.expand_path('..', __dir__))
end

post_install do |installer|
  flutter_post_install(installer)
end
